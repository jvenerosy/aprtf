# API Directus (public - intégré au build)
PUBLIC_HOST_API=http://localhost:8055

# Token d'authentification Directus (privé - runtime uniquement)
# Générer via: Directus > Settings > Access Tokens
DIRECTUS_TOKEN=your-directus-token-here
